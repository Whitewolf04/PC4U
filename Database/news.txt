*Each entry CAN look like this (use \t for delimiters?):
title	descriptionn	url-to-picture
*We can then have newsletter read this file and put the stuff dynamically, so if anything is added/removed, it's reflected.